
@model IEnumerable<Odontologia.Models.RecepcionProducto>

@{

    ViewBag.Title = "Das";
}
<div class="row">
    <div class="col-lg-12" style=" overflow-y:scroll;     height:300px;     ">

        <table class="table">

            <tr>
                <th>


                    @Html.DisplayNameFor(model => model.Cantidad)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Estado)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Empleado.Nombre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Producto.Codigo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Proveedor.Nombre)
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr style="background-color:white">
                    <td>
                        @Html.DisplayFor(modelItem => item.Cantidad)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Estado)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Empleado.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Producto.Codigo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Proveedor.Nombre)
                    </td>
                    <td>
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })|*@
                        @*@Html.ActionLink("Recepcionar", "Confirmar", new { id = item.Id }, new { @class = "myButton" })|*@
                        @*<a href="#" class="myButton">Recepcionar</a>*@
                    </td>
                </tr>
            }

        </table>


    </div>
   
</div>

